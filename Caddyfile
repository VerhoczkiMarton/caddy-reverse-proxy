{
  admin off
  persist_config off
  auto_https off
  log {
    output stdout
    level debug
  }
  servers {
    trusted_proxies static private_ranges
  }
}

:{$PORT} {
  log {
    format json
  }

  # Handle frontend requests only for testing
  handle {
    reverse_proxy {$FRONTEND_DOMAIN}:5173 {
      header_up Host {http.reverse_proxy.upstream.hostport}
    }
  }
}
